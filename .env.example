# PostgreSQL — see spec/environments.yaml
DATABASE_URL=postgresql://lula:lula@localhost:5432/lula_coffee?schema=public

# JWT validation (HS256) — min 32 characters
JWT_SECRET=dev-only-change-me-use-32-chars-min!!
JWT_ISSUER=lula-coffee-api
